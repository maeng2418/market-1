.authPhone
    input.phone(type='text', name="phone" placeholder="휴대폰*", onKeyup="this.value=this.value.replace(/[^0-9]/g,'')")
    button(onClick="confirmPhone()")
        span 인증받기
.authPhoneDesc
    ul
        li 인증번호 전송은 통신사에 따라 최대 1분까지 소요될 수 있습니다.
        li 인증번호가 도착하지 않을 경우 '인증번호 재전송'을 눌러주세요.